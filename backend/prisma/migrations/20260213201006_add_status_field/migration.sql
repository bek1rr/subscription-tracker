BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Subscription] ADD [status] NVARCHAR(1000) NOT NULL CONSTRAINT [Subscription_status_df] DEFAULT 'Active';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
